import { Typography } from "@mui/material";
import { Box } from "@mui/system";

export const Noticias = () => {
  return <Box>
    <Typography>Aqui devem aparecer as notícias relacionadas à instituição, fazendo conexão com o instagram.</Typography>
  </Box>;
};
